### Refresh Token
POST {{baseUrl}}/v1/auth/refresh-token
Content-Type: application/json

{
  "refreshToken": "your-refresh-token-here"
}

### Response example
# {
#   "success": true,
#   "message": "Token refreshed successfully",
#   "data": {
#     "accessToken": "new-access-token",
#     "refreshToken": "new-refresh-token"
#   }
# }

### Notes:
# 1. The base URL is stored in the environment variable {{baseUrl}}
# 2. Replace "your-refresh-token-here" with an actual refresh token
# 3. To change the environment, use the environment selector in the HTTP client
